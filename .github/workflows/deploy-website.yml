name: Build and Deploy Website to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual triggering

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./loggerect-website

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: loggerect-website/package-lock.json

      - name: Clean build directory
        run: |
          rm -rf out .next
          echo "Cleaned build directories"

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint
        continue-on-error: true

      - name: Build Next.js application
        run: npm run build
        env:
          NODE_ENV: production

      - name: Verify build output
        run: |
          if [ ! -d "out" ]; then
            echo "Error: out directory not found after build"
            exit 1
          fi
          echo "Build output verified:"
          ls -la out/ | head -20
          echo ""
          echo "Checking HTML for CSS/JS paths:"
          if [ -f "out/index.html" ]; then
            echo "CSS links:"
            grep -o 'href="[^"]*"' out/index.html | grep -E '\.(css|js)' | head -10 || echo "No CSS/JS links found"
            echo ""
            echo "First 20 lines of index.html head:"
            head -50 out/index.html | grep -A 20 "<head" || head -20 out/index.html
          fi
          echo ""
          echo "Checking for _next directory structure:"
          find out/_next -type f -name "*.css" | head -5 || echo "No CSS files found in _next"
          find out/_next -type f -name "*.js" | head -5 || echo "No JS files found in _next"

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: loggerect-website/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
